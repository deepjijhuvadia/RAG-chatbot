# RAG-chatbot
Hello Community
to run the project you need to have 
!pip install langchain --upgrade
!pip install pypdf
!pip install git+https://github.com/theskumar/python-dotenv.git
!pip install unstructured
!pip install pdfminer
!pip install pinecone-client
!pip install openai
!pip install typing_extensions
!pip install vectorstore

if using colab just run all sections
 
 after installing 
 go to openai and get your api key
 and go to pinecone and get create a new index and uplode your traing data in it as a pdf
 
